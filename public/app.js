const modal=document.querySelector('#simulationModal');const closeModal=document.querySelector('#closeModal');document.body.classList.add('modal-open');closeModal.addEventListener('click',()=>{modal.classList.add('hidden');document.body.classList.remove('modal-open');setTimeout(()=>modal.remove(),350)});modal.addEventListener('click',e=>{if(e.target===modal)closeModal.click()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&document.body.classList.contains('modal-open'))closeModal.click()});

const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));

const track=document.querySelector('#carouselTrack');const cards=[...document.querySelectorAll('.stage-card')];const progress=document.querySelector('.progress i');let current=0;function moveCarousel(direction){current=(current+direction+cards.length)%cards.length;const gap=24;track.style.transform=`translateX(-${current*(cards[0].getBoundingClientRect().width+gap)}px)`;progress.style.transform=`translateX(${current*100}%)`}document.querySelectorAll('.carousel-controls button').forEach(button=>button.addEventListener('click',()=>moveCarousel(Number(button.dataset.dir))));window.addEventListener('resize',()=>moveCarousel(0));

const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.site-header nav');menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav.classList.toggle('mobile-open',!open)});
